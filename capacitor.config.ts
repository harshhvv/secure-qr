import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.ionic5',
  appName: 'ionic5',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
